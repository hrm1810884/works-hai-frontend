import { style } from "@vanilla-extract/css";

export const iconButtonStyle = style({
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    padding: "10px",
    width: "fit-content",
    height: "fit-content",
});
