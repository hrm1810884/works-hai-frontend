import { ActionIcon, Menu, MenuTarget } from "@mantine/core";
import React, { FC } from "react";

import { vars } from "@/styles";

type props = {
    contentsElement: React.ReactElement;
};

export const ToolButtonWithFunction: FC<props> = (props) => {
    const { contentsElement } = props;

    return (
        
    );
};
