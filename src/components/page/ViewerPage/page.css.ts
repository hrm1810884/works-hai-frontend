import { style } from "@vanilla-extract/css";

export const wrapper = style({
    display: "flex",
    overflow: "scroll",
    justifyContent: "center",
    alignItems: "center",
    width: "100vw",
    height: "100vh",
});
